/// <reference types="vitest/globals" />
// Makes the global `describe`/`it`/`expect` (vitest, configured with globals: true)
// available to TypeScript for the pure-logic test files (helpers.test.ts, validation.test.ts,
// client.test.ts, resolver.test.ts). See vitest.config.ts.
